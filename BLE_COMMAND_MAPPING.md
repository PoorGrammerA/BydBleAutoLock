# 차량 BLE 명령 매핑 및 검증 현황

이 문서는 BYD Watch BLE 키 인증이 완료된 뒤 사용하는 로컬 BLE 제어 명령을 기록한다.

- 서버의 `functionNo`와 BLE `functionId`는 서로 다른 값이다.
- BLE 명령은 6번 연결·인증이 `READY` 상태일 때만 전송한다.
- 아래 결과는 현재 차량에서 사용자가 확인한 결과다. 다른 차종·소프트웨어 버전에서는 달라질 수 있다.
- 실제 MAC 주소, VIN, 토큰, dkey는 이 문서에 기록하지 않는다.

## 현재 앱에 남긴 BLE 버튼

### 고정 BLE 제어 버튼

`cfFixedList`를 파싱해 UI를 만들지 않는다. 앱은 아래 다섯 개의 고정 BLE 버튼만 표시한다.

| 버튼 | BLE functionId | 제어 코드 | 상태 |
| --- | ---: | ---: | --- |
| `Locking - BLE` | `9002` | `0x07` | 동작 확인 |
| `Unlocking - BLE` | `9001` | `0x05` | 동작 확인 |
| `Open the trunk - BLE` | `9015` | `0x06` | 동작 확인 |
| `only light - BLE` | `9010` | `0x16` | 동작 확인 |
| `light and honking - BLE` | `9005` | `0x0A` | 동작 확인 |

버튼은 6번 BLE 인증이 완료된 뒤에만 활성화된다.

검증된 명령은 차량 notify 응답의 `24 E5 <제어 코드> 01` 형식까지 확인됐다. 앱은 로컬 GATT 쓰기 완료만으로 성공 처리하지 않고, 최대 3초 안에 이 차량 응답이 도착해야 성공 토스트와 자동 잠금·해제 진동을 발생시킨다.

### Raw BLE 트렁크 제어 및 테스트

확정된 트렁크 열기와 추가 확인이 필요한 열기·닫기 명령을 `Raw BLE function-ID test` 섹션에 남겨 두었다. 6번 인증 뒤 확인 대화상자를 거쳐 차량으로 직접 전송된다.

| 기능 | BLE functionId | 제어 코드 | 상태 |
| --- | ---: | ---: | --- |
| 전동 트렁크 열기 | `9015` | `0x06` | 동작 확인 |
| 전동 트렁크 열기 테스트 | `9019` | `0x1A` | 동작 여부 재확인 필요 |
| 전동 트렁크 닫기 테스트 | `9020` | `0x18` | 동작 여부 재확인 필요 |

주차 상태에서 주변 사람·장애물을 확인한 뒤 한 번씩만 테스트한다.

## 코드가 확인됐지만 UI에서 직접 노출하지 않는 항목

| BLE functionId | 제어 코드 | 해석 | 현재 처리 |
| ---: | ---: | --- | --- |
| `9011` | `0x06` | 뒷문 잠금 해제 추정 | `9015`와 같은 제어 코드지만 별도 기능은 미확정 |

`9015(0x06)`는 현재 차량에서 전동 트렁크 열기로 동작 확인되어 고정 BLE 버튼과 Raw 버튼에 사용한다. `9011`도 같은 제어 코드를 반환하지만 별도 의미는 확인되지 않아 노출하지 않는다.

## 제거한 BLE 후보 매핑

다음 명령은 조사 과정에서 확인한 매핑을 Pure Java 코드에 기록했지만, 현재 차량에서 기대한 동작이 확인되지 않아 BLE UI와 서버 기능 매핑에서 제거했다.

| 기능 후보 | BLE functionId | 제어 코드 | 처리 |
| --- | ---: | ---: | --- |
| 공조 켜기 | `9003` | `0x03` | 제거 |
| 창문 올리기 | `9007` | `0x0C` | 제거 |
| 원키 종료 | `9008` | `0x09` | 제거 |
| 공조 끄기 | `9009` | `0x08` | 제거 |

이 명령들은 차종별 지원 기능, 추가 파라미터, 차량 상태 조건이 필요할 수 있다. 공식 앱의 동일 기능 BLE 프레임을 확보하기 전에는 다시 노출하지 않는다.

## 검증 원칙

1. 차량을 주차하고 주변 안전을 확인한다.
2. 1~6번 인증 과정을 완료해 `READY` 상태인지 확인한다.
3. 한 번에 하나의 명령만 전송한다.
4. 차량의 실제 동작과 앱 6번 로그를 함께 기록한다.
5. 새 매핑은 실제 차량 동작과 프레임 근거가 모두 있을 때만 고정 BLE 버튼에 추가한다.

## 참고

- 현재 제어 코드 변환과 프레임 생성은 프로젝트 자체 네임스페이스의 `BydBleCodec`과 `VehicleBleCommand`가 수행한다. 네이티브 라이브러리는 사용하지 않는다.
- 서버 REST 원격 제어와 BLE 제어는 별개다. REST 명령은 BYD 서버를 거치고, 이 문서의 BLE 명령은 인증된 차량 BLE 연결로 직접 전송된다.
